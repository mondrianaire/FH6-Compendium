# Project memory (mirror)

A **read-only snapshot** of this project's assistant auto-memory, copied here so the
knowledge base is versioned with the code. The **live source of truth** is the
per-user auto-memory at `~/.claude/projects/.../memory/` — edit there, not here;
re-run the mirror to refresh this copy.

Mirrored 2026-09-16. `MEMORY.md` is the index. Notes use `[[wikilink]]`
cross-references and YAML frontmatter from the memory system; they render as plain
Markdown here.
