---
name: jett-tuning-workflow
description: "Jett now tunes cars from scratch (FH6 locks downloaded tunes — can't view or modify them); app doctrine shifted from tune-codes to baselines+calculator; locked tunes ARE reverse-engineerable (build_forensics protocol)"
metadata: 
  node_type: memory
  type: user
  originSessionId: 87e277b2-0c63-4ece-b224-d3c5a84d0789
  modified: 2026-09-03T18:57:24.301Z
---

WEALTH STATE (2026-08-01): Jett has 200M+ credits banked — price is NO LONGER a constraint. Recommend by AVAILABILITY, never cost: Autoshow = instant top tier at any price; "free" journal rewards rank BELOW Autoshow (they cost play time); only luck-gates (wheelspin/limited-time) and real-money DLC remain real barriers. Don't pitch credit farms or budget alternatives unless asked. P2W DOCTRINE (2026-08-10): real-money cars are PERMANENTLY off the table — Jett will never buy them; never recommend one as a pick (mention only as "the P2W option exists" context). Dashboard strikes them red (isP2W: curated acquisition_difficulty === "premium" only — free-text sniffing false-positives, e.g. "Vip" inside "Viper").

As of 2026-07-27 Jett is actively learning to tune cars themselves and builds every tune FROM SCRATCH. Reason (player-confirmed game fact): FH6 downloaded tunes are LOCKED — you cannot view their settings or modify them, so a downloaded code can be driven but never used as a starting point or reference.

AMENDMENT 2026-09-03 (supersedes the framing below): the 2026-08-10 amendment describes what Jett sees IN-GAME — the lock hides slider values from the player's own screen. It does NOT describe what this project's OWN TOOLING can see. Per [[fh6-savefile-tune-decode]] and [[fh6-slider-range-derivation]] (both established after this memory), every locked/downloaded tune's sliders decode directly from the save file's own bytes, at full precision, with zero driving — the lock byte gates in-game editing only, never this project's own decode. A dashboard bug asserting "sliders are hidden by the lock" was found and fixed 2026-09-03 (see [[fh6-slider-value-reliability-hierarchy]]) precisely because this distinction wasn't kept sharp downstream. The forensics protocol below (parts via compare-pane/My-Cars, sliders via driving) is the FALLBACK for before Jett has installed the tune at all — once it's on disk, decode the file directly; don't reach for driving forensics first.

AMENDMENT 2026-08-10 (original — now the pre-install fallback case, not the general rule): locked ≠ opaque. The lock hides only SLIDER VALUES — the PARTS LIST is recoverable. The tune-browser compare pane (pre-download, any tune) shows Previous-vs-New for 13+ stats including outright Suspension/Tire Compound/Drivetrain rows; My Cars stat pages (post-download) add displacement/front%/weight; driving forensics (engine audio, HUD gear count, RPM@speed) fill the rest. Full protocol = `build_forensics` in data/tuning-test-battery.json (case study: ford351guy's Viper GTS ACR '99 "S1 Goliath"). Workflow: decode parts → replicate on a second copy → Jett fits their own sliders to the proven chassis. DOCTRINE (Jett-stated): the deliverable of any build decode is the DISCRETE PARTS MANIFEST — slider values are ephemeral (Jett re-fits them anyway); the tuner's intelligence lives in the PI allocation across parts. Record decoded builds as manifests with per-part evidence tiers. REPLICATION IS SCAFFOLDING, NOT DESTINATION (4C session 2026-08-10): Jett decodes builds to learn the allocation, then FORKS toward their own taste — they may dislike how the reference car drives (they rejected the LogikJ 4C's handling from the start). Never treat the reference build's behavior as the success criterion or ask them to benchmark against it; 'a car I like driving' is the goal.

**Why:** This obsoletes the project's original tune_sourcing_policy premise ("you won't self-tune — the app hands you downloadable codes"). Codes still matter for quick wins (Rivals ghosts, drag), but the growth path is self-tuning.

TRANSMISSION (2026-08-10): Jett plays AUTOMATIC — never advise holding/choosing gears manually. Auto-compatible equivalents: progressive throttle (Forza autos kick down on throttle demand — a stomp = downshift = torque dump), taller final drive (raises every gear the auto can pick; Jett discovered this independently on the 4C), lengthening mid-gears for corner-exit torque, TCS as a legitimate consistency option despite board meta running it off.

**How to apply:** When recommending tunes, lead with modifiable starting points, not codes: per-car tune_baseline blocks, the tuning-variables tab (tune order + poles + slider ranges), the discipline templates tab, and the forza.tools calculator (state is URL-encoded — share links that pre-fill it). Jett's own tunes are the only editable ones; offer to log them as entries (they become player-verified by definition). Slider-range fields in forza.tools want the car's in-game MIN-MAX, not current values — Jett got this wrong once ([[fh6-afk-macro-stack]] unrelated; see honda-beat session 2026-07-27). Related: [[fh6-skill-farm-map]].
